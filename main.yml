vars:

    # renovate: datasource=docker depName=jenkins/jenkins
    jenkins_server_version: '2.414.1-lts-alpine'

    vault-agent:
        # renovate: datasource=docker depName=hashicorp/vault-enterprise
        vault_agent_version: '1.12.11-ent'
        